---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "uptrace_team_project Resource - uptrace"
subcategory: ""
description: |-
  Grants a team access to a project. Idempotent — creating twice is a no-op server-side. Delete revokes access without touching the team or project.
---

# uptrace_team_project (Resource)

Grants a team access to a project. Idempotent — creating twice is a no-op server-side. Delete revokes access without touching the team or project.

## Example Usage

```terraform
resource "uptrace_org" "example" {
  name = "My Organization"
}

resource "uptrace_project" "example" {
  org_id = uptrace_org.example.id
  name   = "api"
}

resource "uptrace_team" "example" {
  org_id = uptrace_org.example.id
  name   = "platform"
}

resource "uptrace_team_project" "example" {
  org_id     = uptrace_org.example.id
  team_id    = uptrace_team.example.id
  project_id = uptrace_project.example.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `org_id` (String) Organization ID the team belongs to. Changing this forces recreation.
- `project_id` (String) Project ID to grant the team access to. Changing this forces recreation.
- `team_id` (String) Team ID. Changing this forces recreation.

### Read-Only

- `id` (String) Composite identifier (equals project_id). Unique within the parent team.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import uptrace_team_project.example 1:123:456
```
